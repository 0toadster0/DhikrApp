/** Fixed reading-time estimate for the reflection comparison only (hours). */
export const SCREEN_TIME_REFLECT_QURAN_HOURS = 40;
/** Lifetime window baked into the simplified formula (years). */
export const SCREEN_TIME_REFLECT_LIFETIME_YEARS = 60;

export const SCREEN_TIME_REFLECT_COUNT_STAGGER_MS = 260;
export const SCREEN_TIME_REFLECT_COUNT_DURATION_MS = 360;
